image: node:latest

stages:
  - test
  - release

before_script:
  - npm install -g pnpm
  - pnpm install

test:
  stage: test
  script:
    - pnpm test

publish:
  stage: release
  script:
    - pnpm build
    - pnpm release
